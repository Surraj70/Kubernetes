apiVersion: batch/v1
kind: CronJob

metadata:
  name: minute-backup
  namespace: nginx

spec:
  schedule: "* * * * *"

  jobTemplate:
    spec:
      template:
        metadata:
          name: minute-backup
          labels:
            app: minute-backup

        spec:
          restartPolicy: OnFailure

          containers:
            - name: backup-container
              image: busybox
              command:
                - sh
                - -c
                - |
                  echo "Backup Started"
                  mkdir -p /backups
                  mkdir -p /data
                  cp -r /data /backups
                  echo "Backup Completed"

              volumeMounts:
                - name: data-volume
                  mountPath: /data

                - name: backups-volume
                  mountPath: /backups

          volumes:
            - name: data-volume
              hostPath:
                path: /data
                type: DirectoryOrCreate

            - name: backups-volume
              hostPath:
                path: /backups
                type: DirectoryOrCreate
